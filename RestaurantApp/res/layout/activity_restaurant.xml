<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp"
    android:background="#2bb38f"
    android:gravity="center_horizontal" >
        
	<ImageView
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:src="@drawable/logo"
	    android:layout_alignParentTop="true"
	    android:contentDescription="@string/logoAlt"
	     />

	    <ScrollView
	        android:layout_marginTop="50dp"
	        android:layout_width="match_parent"
	        android:layout_height="match_parent" >

			<LinearLayout 
			    android:layout_width="match_parent"
			    android:layout_height="wrap_content"
			    android:orientation="vertical"
			    android:gravity="center">
			    <Button 
			        android:id="@+id/tableReady"
			        android:drawableTop="@drawable/table"
			        android:text="@string/tableReady"
			        style="@style/homeButtons" />
			    <Button 
			        android:id="@+id/ordersbtn"
			        android:drawableTop="@drawable/worder"
			        android:text="@string/takeOrder"
			        style="@style/homeButtons" />
			    <Button 
			        android:id="@+id/alertbtn"
			        android:drawableTop="@drawable/worder"
			        android:text="@string/Alert"
			        style="@style/homeButtons" />
			</LinearLayout>
	    </ScrollView>
</RelativeLayout>
